<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeaseLinkApplication">

        <!-- Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name=".ApplicationActivity" />
        <activity android:name=".CardPaymentActivity" />
        <activity android:name=".ConfirmationActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".LandingActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MaintenanceRequestActivity" />
        <activity android:name=".PaymentsActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".PropertyInfoActivity" />
        <activity android:name="ReviewEscalationActivity"/>
        <activity android:name="student.projects.leaselinkapplication.ManagerLanding.ManagerActivity"/>
        <activity android:name="student.projects.leaselinkapplication.ManagerLanding.SendBroadcastActivity"/>
        <activity android:name="student.projects.leaselinkapplication.ManagerLanding.SendReminderActivity"/>
        <activity android:name="student.projects.leaselinkapplication.ManagerLanding.UpdateListingsActivity"/>

        <!-- Optional: Preloaded fonts -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>